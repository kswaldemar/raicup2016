#!/bin/bash

# Create zip file to upload into russianaicup.ru
# Should be called from mystrategy dir

mkdir -p @RELEASE_DIR@

VER=`git describe --tags --always`
FNAME=${VER}.zip

rm -f @RELEASE_DIR@/${FNAME}

echo "Creating ${FNAME}"

zip @RELEASE_DIR@/${FNAME} @sources@ @headers@